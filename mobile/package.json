{
  "name": "library-management-mobile",
  "private": true,
  "version": "0.1.0",
  "description": "Capacitor shell for Library Management (phone / tablet / iPad testing)",
  "scripts": {
    "prepare-config": "node scripts/prepare-config.js",
    "cap:sync": "npm run prepare-config && npx cap sync",
    "cap:sync:android": "npm run prepare-config && npx cap sync android",
    "cap:sync:ios": "npm run prepare-config && npx cap sync ios",
    "build:apk": "./scripts/build-apk.sh"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.8",
    "@capacitor/core": "^5.6.0",
    "@capacitor/ios": "^5.7.8"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.6.0"
  }
}
