version: '3.8'

name: library-management

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3100:3000"  # Map to Next.js default port
    environment:
      - NODE_ENV=development
      - PORT=3000
      - NEXT_PUBLIC_API_URL=http://localhost:4000/api
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    networks:
      - library-network
    container_name: library-management-frontend
    labels:
      - "com.docker.compose.project=library-management"
      - "com.docker.compose.service=frontend"
      - "app.name=library-management"
      - "app.component=frontend"
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "4000:4000"
    volumes:
      - ./backend/src:/app/src
      - ./data:/app/data
    environment:
      - NODE_ENV=development
      - PORT=4000
      - SQLITE_PATH=/app/data/library.sqlite
      - JWT_SECRET=development-secret-key
      - CORS_ORIGIN=http://localhost:3100
    networks:
      - library-network
    container_name: library-management-backend
    labels:
      - "com.docker.compose.project=library-management"
    restart: unless-stopped

  # nginx:
  #   build:
  #     context: ./nginx
  #     dockerfile: Dockerfile
  #   ports:
  #     - "9080:80"
  #     - "9443:443"
  #   volumes:
  #     - ./nginx/ssl:/etc/nginx/ssl
  #   environment:
  #     - NGINX_ENV=development
  #   depends_on:
  #     - frontend
  #     - backend
  #   networks:
  #     - library-network
  #   container_name: library-management-nginx
  #   labels:
  #     - "com.docker.compose.project=library-management"
  #     - "traefik.enable=true"
  #     - "traefik.http.routers.library-nginx.rule=Host(`library.localhost`)"
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost/health"]
  #     interval: 30s
  #     timeout: 3s
  #     retries: 3

networks:
  library-network:
    name: library-management
    driver: bridge
    labels:
      - "com.docker.compose.project=library-management"
